{
  "name": "icbe",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "wrangler": "^4.59.1"
    }
  },
  "scripts": {
    "lint": "biome lint",
    "format": "biome format --write",
    "build:userscript": "bun run -F './packages/colorproven-userscript' --elide-lines 0 build",
    "build:apps": "bun run -F './apps/*' --elide-lines 0 build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.11",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "esbuild": "latest"
  }
}
